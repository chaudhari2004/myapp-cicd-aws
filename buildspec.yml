version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.10
  build:
    commands:
      - echo "Build Started"
      - docker build -t myapp .
      - docker run myapp
artifacts:
  files:
    - '**/*'
